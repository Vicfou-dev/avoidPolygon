class Path {
    optimize(path, obstacles) {
        for(var i = 0; i < path.length; i++) {
            var segment = new Segment(path[i][p1], path[i + 1][p2]);
            if(segment.cross(polygon)) {
            }
        }
    }
}